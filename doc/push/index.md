# 推送: 先把数据切成桶, 每桶单独选一类

> **这套 `push/` 目录现在是"另一种排法"的草稿, 不是新的事实来源。**
> 事实来源仍然是 [`../pushDesignAndReview.md`](../pushDesignAndReview.md)(以下简称**手册**)。
> 这里只做一件事: 把**按类分叉**的那部分内容, 从手册的五个小节里抽出来, 按类重新码一遍,
> 方便"我这一桶要怎么做"这个问题一次读完。
> 两边都存在期间, **改规则请改手册**, 这里跟着改。要不要把手册那几节换成指到这里的链接, 待定。

## 这套目录里有什么

| 文件 | 装什么 |
| --- | --- |
| `index.md`(本篇) | **选型**: 三句话决定一桶数据属于哪一类; 四类横向对照; 一个填好的例子 |
| [`protocol.md`](protocol.md) | **四类共用的协议**: 帧信封预算 / 帧类型清单 / 房间句柄 / 纪元 / `NeedFullReload` |
| [`class1TextStream.md`](class1TextStream.md) | 类一 正文流(字节流追加) |
| [`class2MsgStream.md`](class2MsgStream.md) | 类二 消息流(离散 id 追加) |
| [`class3FullPush.md`](class3FullPush.md) | 类三 全量推(整份覆盖 + 外生号) |
| [`class4Poke.md`](class4Poke.md) | 类四 戳一下(没有内容, 只有一句"你去拉") |

**每个类文件的结构是一样的**: 什么时候选它 → 帧字段 → 服务端怎么算这一份 → 客户端怎么应用
→ 这一类的降级路径 → 这一类的典型翻车和必须有的测试场景。

## 不在这套目录里的东西(而且**故意**不在)

这些是四类**共用**的, 复制成四份必然漂移, 所以只留在手册里:

| 是什么 | 在手册哪 |
| --- | --- |
| 五个指标的顺序和取舍规则 | §1 |
| 房间状态 = 不可变快照 + 原子指针 | §4.1 |
| 进房三步, 锁的边界画在第一步和第二步之间 | §4.2 |
| 广播写侧顺序 / 脏标记必须在读库前清 | §4.3 |
| **写缓冲一笔账, 装不下就断线** | §4.4 |
| 锁清单 / goroutine 停机 | §4.5 §4.6 |
| 连接: ping/pong / 无限重连 / **断线清空句柄表** | §5.1 |
| 进房 pending 缓冲与重放(四桶共用一个缓冲) | §5.2 |
| 参数表 + 两条参数不变式 | §6 |
| 降级路径三分组的**判据** | §7 |
| 测试纪律 / 验收清单 27 条 | §8 §9 |

> 有几条规矩**只有横跨四类才成立**, 它们也只能留在手册里:
> 进房窗口那个场景要**三桶各压一遍**(一桶的 id 写错会被另一桶掩盖);
> 类一跨命名空间的回退靠的是 §4.3 的"Store 必配广播"(那是服务端的规矩, 不属于任何一类);
> 帧信封那 64 字节是四类共用的一个预算。

---

## 1. 选型: 三句话, 按顺序问, 第一个"是"就定了

**顺序不能换。**

**问题 1: 这一桶只在尾部追加, 从不重写中间, 也从不变短吗?**

* 是 + 载体是**连续字节流** → **[类一 正文流](class1TextStream.md)**
* 是 + 载体是**一条条自带全序 id 的条目** → **[类二 消息流](class2MsgStream.md)**

**问题 2: 整份推下去也不心疼吗?**(经验红线: 单份几 KB 以内, **且按"每帧"算不是按"一次"算**)

* 是 → **[类三 全量推](class3FullPush.md)**
* **问题 2b(问题 2 答"是"之前, 先插一句): 这一桶是不是"变得很勤 + 只关心最新值 + 差一两秒没人在乎"?**
  典型长相: **每秒都在变、每次都得整份带、而且用户根本分不清 0 秒和 1 秒的区别** ——
  动画式的进度、光标闪烁、会整屏重绘的终端输出、跑马灯式的计数器。
  它答"是"就**不能进类三** —— 类三的成本是 `变更次数 × 整份大小`, 这两个因子它都顶满了,
  于是它会**霸占每一帧的信封**, 把真正高频的那一桶(类一)的带宽账吃掉。
  **正确的动作是"把它从每帧的信封里拿出去", 而不是"改成轮询"。** 两条出路, 按这个顺序选:

  **(a) 首选: 退到[类四(戳一下)](class4Poke.md), 并靠客户端合并连续的戳。**
  成本随"**真的在看的人**"走 —— 没人打开那个面板就一次查询都不发生, 而且合并间隔就是延时旋钮。

  **(b) 只有在"上游根本没有可靠的 fire 点"时(说不清它什么时候变了, 所以戳不出来),
  才退到前端定时轮询**: 串行循环(**不是 `setInterval`**, 否则慢查询会叠罗汉)+ 版本号短路"没变就别重传"。

  > **(b) 是一次优先级倒挂, 必须当场写下理由才允许走。**
  > 它是整套设计里**唯一一个 O(在线人数) 的常驻负载** —— 顶在**并发**(第 3 优先级)上,
  > 换来的只是省**带宽**(第 4 优先级)。手册 §1.1 的取舍规则本来不允许这么做。
  > 走 (b) 的四个条件, 缺一条就回 (a): ①查询是纯内存或极便宜; ②只有打开那个面板的人才拉;
  > ③有版本号短路; ④串行循环。一旦它变成一次真查库, **它就是性能炸弹**, 必须回 (a)。

**问题 3: 都不是 → [类四 仅戳一下](class4Poke.md)**, 客户端自己去拉全量。

> 违反顺序的后果: 能追加的东西用类三 = 每次把全文重推一遍 = O(n²);
> 整份才几百字节的东西硬做追加 = 发明一套 id 和一堆缺口判断, 换来省几十字节。

完整判据(两句都为是才值得推内容): **变更描述的大小 << 重算全量的大小**,
且**客户端能原地应用这个描述**。类一类二是"远小于", 类三是"描述就是全量, 但全量本来就小",
类四是"描述比全量还贵, 或者根本说不清"。

**一个房间里混用多类是常态, 不是妥协。** 不要试图让整个房间共用一套 id。

> 违反后果: 合成一套 id 就必然要一把包住 DB IO 的锁; 而且一桶判错会表现成"整个房间不收敛",
> 分不出是哪一桶。

---

## 2. 四类横向对照(贴墙用)

| 维度 | 类一 正文流 | 类二 消息流 | 类三 全量推 | 类四 戳一下 |
| --- | --- | --- | --- | --- |
| **id 从哪来** | 数据自己(字节长度) | 数据自己(id 区间) | 外生计数 + 纪元 | 没有 id |
| **服务端要锁吗** | 不要 | 不要 | 只锁那一个整数 | 不要 |
| **客户端动作** | 裁剪后追加 | 去重后追加 | 整份覆盖 | 去发一个拉取请求 |
| **应用幂等吗** | 不幂等(靠偏移) | 幂等(靠 id 去重) | 幂等 | 幂等 |
| **丢一帧的后果**<br>(**只发生在断线之后**;<br>连接活着时不丢帧) | 接不上 → 重新进房 | 接不上 → 重新进房 | 下一帧盖回来(自愈) | 停在旧数据(等下次戳)<br>重连会重进房, 也自愈 |
| **溢出/太大时** | 申报基准 + 回差值<br>**不许整页重拉** | 分页 + 整页重拉<br>(重拉有效) | 退回类一或类四<br>重新走选型 | (本来就没内容) |
| **带宽** | O(n), 常数近 1 | O(n) | O(变更次数 × 整份) | O(变更次数 × 全量) |
| **额外 RTT** | 0 | 0 | 0 | 1 |
| **典型翻车** | 错位且静默 | 漏一条且无人发现 | 旧数据贴新号 | 被按 id 丢掉 |

**一条贯穿四类的判据: 只有类三需要一个外生的号, 也就只有它需要一个受保护的整数。**
任何时候你发现类一 / 类二也想加锁, 那说明你在给它们发外生号 —— 回上面重新找它们的内生号。

---

## 3. 一个填好的例子

一个 AI 编程助手的 web 前端, 一个页面同时进 5 个房间:

| 桶 | 装什么 | 类别 | id 字段 | 客户端动作 |
| --- | --- | --- | --- | --- |
| 会话详情·正文 | AI 流式吐字 / 工具调用交错正文 | 类一 | `CallSeq` + `BaseLen` + `Text` | 按偏移裁剪拼接 |
| 会话详情·消息 | 聊天消息列表 | 类二 | `BaseMsgId` + `NewMsgs` | 按 id 去重追加 |
| 会话详情·状态 | 会话/计划/任务列表等约 20 个标量(三个库拼出来的) | 类三 | `Seq` | 整份覆盖 |
| 全局状态 | "运行中 / 排队中"两个数字 | 类三 | `Seq` | 整份覆盖 |
| 会话列表 / 任务列表 / 建任务 | 分页查询结果 | 类四 | 无 | 去拉一次全量 |
| (终端输出黑框) | TUI 尾部十几行, 会整屏上滚, 光标一直在闪 | **不进推送**(2b) | 版本号短路 | 前端 1 秒一轮拉 |

**最后一行是 2b 的实例, 但它走的是 (b) 那条退路, 不是首选的 (a)。**
它曾经放在类三里, 每帧从 37 字节涨到 1183 字节 —— 一桶"看着才 1KB"的东西, 因为**每帧都在变**,
把整条链路的带宽账吃掉了; 拆出推送之后帧回到 37 字节。
**但记录里没写当时为什么选轮询而不是 (a) 的戳一下**, 所以别把这一行当成"轮询是推荐做法"的依据 ——
新的一桶数据碰到同样的形状, 按 2b 的顺序先试 (a)。

顺带: **"统一形状"本身没有价值, 省下来的字节和 RTT 才有。**
